# Vercel builds the real app (src) into /app and serves the phone-frame site at /.
# Keep only what the build needs; drop the native shells, backend, and heavy dirs.
node_modules
dist
android
ios
supabase
.github
release
electron
mockups
*.md
