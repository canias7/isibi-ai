# Vercel builds the real app (src -> dist) and serves it at the root.
# Keep only what the build needs; drop native shells, backend, and heavy dirs.
# (Don't ignore *.md — src/legalDocs.ts imports legal/PRIVACY.md & TERMS.md.)
node_modules
dist
android
ios
supabase
.github
release
electron
mockups
